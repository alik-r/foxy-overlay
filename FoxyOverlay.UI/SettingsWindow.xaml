<Window x:Class="FoxyOverlay.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:FoxyOverlay.UI"
        mc:Ignorable="d"
        Title="Settings" Height="400" Width="600">
    <Grid>
        <StackPanel>
            <!-- TODO: replace with real controls bound to the VM properties -->
            <TextBlock Text="(Settings form goes here)" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,20">
                <Button Content="Save"    Command="{Binding SaveCommand}"    Width="100" Margin="0,0,10,0"/>
                <Button Content="Cancel"  Command="{Binding CancelCommand}"  Width="100" Margin="0,0,10,0"/>
                <Button Content="Defaults" Command="{Binding ResetDefaultsCommand}" Width="100"/>
            </StackPanel>
            <TextBlock Text="Logs:" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding Logs}" Height="200"/>
        </StackPanel>
    </Grid>
</Window>
